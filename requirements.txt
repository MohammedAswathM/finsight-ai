# =============================================================================
# FinSight AI — Python dependencies
# Python 3.11+
# =============================================================================

# --- Core Agentic Framework ---
langchain>=0.2.0,<0.3.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.5
langgraph>=0.1.0

# --- Vector Database / RAG (Member 1) ---
chromadb>=0.5.0
pypdf
sentence-transformers
# ParentDocumentRetriever + ContextualCompression need these
rank-bm25

# --- Data & SQL (Member 2) ---
yfinance
pandas
pandas-ta
SQLAlchemy
matplotlib

# --- Scientific stack (pin for Windows binary compatibility) ---
# SciPy wheels currently require NumPy < 2.3; pin avoids sklearn/transformers import crashes.
numpy<2.3.0
scipy<1.14.0

# --- ML Models (AIML Infra) ---
# Fraud detection (Member 1)
xgboost
lightgbm
imbalanced-learn
scikit-learn
shap
# FinBERT sentiment (Member 2)
transformers
torch
datasets==2.19.2
accelerate

# --- HF stack pins (avoid breaking transformers + mlflow on Windows) ---
huggingface-hub<1.0.0
pyarrow<24

# --- pandas pin (mlflow + many deps currently assume pandas<3) ---
pandas<3
# Forecaster (Member 3) uses lightgbm above

# --- MLOps ---
mlflow

# --- Evaluation (Member 4) ---
ragas

# --- UI (Member 4) ---
gradio

# --- News / Sentiment Agent (Member 4) ---
feedparser

# --- Additional requirements for local ingestion and HTML parsing ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Environment & Utilities ---
python-dotenv
